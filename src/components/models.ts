export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface Thumbnail {
  title: string;
  author: string;
  watchNumber: string;
  uploadTime: string;
}
